<?php

$arr = ['Понедельник', 'Вторник',
        'Среда', 'Четверг', 'Пятница',
        'Суббота', 'Воскресенье'];

$day = $arr[Date('w') - 1];

foreach ($arr as $num => $d) {
    if ( $d == $day) {
        $d = '<i>'.$d.'</i>';
    }
    echo $d." ";
}
